input {
  beats {
    port => 5044
  }
}

filter {
  # you can add grok/kv/json filters here later if you want
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "universal-%{+YYYY.MM.dd}"
  }
  # stdout { codec => rubydebug } # debug
}
